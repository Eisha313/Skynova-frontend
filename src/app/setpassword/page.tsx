import SideLayout from '../components/sideLayout'; // Adjust the path if necessary
import SetPassword from '../components/setpassword'; // Adjust the path if necessary

export default function ChangePasswordPage() {
  return (
    <SideLayout>
      <SetPassword />
    </SideLayout>
  );
}
